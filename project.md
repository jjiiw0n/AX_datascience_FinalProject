# 지능형 웹 모니터링 자동화 프로젝트

## 1. 프로젝트 개요
지정한 웹사이트를 주기적으로 모니터링하여 새로운 게시물을 감지하고, 이를 지정된 전용 계정으로 기록 및 알림을 보내는 자동화 시스템 구축.

## 2. 시스템 아키텍처 (v8 클라우드 기반 다중 사용자 모델)
- **탐색 (Cloud Scraper - `monitor_v2.js`):** Playwright를 사용하여 클라우드 환경(GitHub Actions)에서 사이트 접속 및 HTML 다운로드.
- **데이터 추출 (AI-Enhanced):** 다운로드된 HTML에서 공고 제목과 링크를 추출할 때 AI를 활용.
- **데이터베이스 (Cloud DB - Supabase):** 로컬 파일(`history.json`) 기반에서 Supabase 관계형 데이터베이스로 전환하여 다중 사용자 데이터(구독자, 관심 사이트, 히스토리) 관리.
- **알림 및 리포팅 (Pure Code - `monitor_v2.js`):** Nodemailer를 사용하여 사용자별 맞춤 이메일 발송.
- **운영 환경:** 로컬 윈도우 스케줄러에서 GitHub Actions(CI/CD) 기반 무인 자동화 환경으로 전환.

## 3. 구축 단계
1. **인증 및 보안 설정:** 전용 모니터링 계정 분리 및 OAuth 2.0 자동화 환경 구축.
2. **수집 로직 최적화:** Playwright 기반의 0-Token 로컬 크롤링 엔진 구축.
3. **AI 파싱 엔진 도입:** 사이트 개편에 대응하기 위한 AI 기반 데이터 추출 로직 구현.
4. **동적 알림 시스템 고도화:** 신규 소식 유무에 따라 헤더 색상이 변하는 지능형 통합 리포트 구현.
5. **스케줄러 안정화:** `run_monitor.bat`을 통한 순차적 스크립트 실행 환경 구축.

## 4. 현재 진행 상태 및 확인 완료 사항
- **진행 상태:** 
  - [x] v7 하이브리드 아키텍처(AI 파싱 + 코드 리포팅) 전환 완료.
  - [x] v8 클라우드 기반 다중 사용자 모델로 전환 완료.
  - [x] Supabase 도입 및 GitHub Actions 자동화 완료.
  - [x] 전용 모니터링 계정(`jeew0n.lee.217@gmail.com`) 발신/수신 연동 완료.

## 5. 향후 로드맵 (Future Roadmap)
- **[Step 1] MVP 웹 대시보드 구축:** 사용자가 직접 관심 정보를 등록할 수 있는 웹 UI(Next.js + Vercel) 개발.
- **[Step 2] 모니터링 대상 지속 확장:** 관심 기업 및 공공기관 공고 추가 등록.
- **[Step 3] AI 직무 적합성 분석:** Agent가 공고 내용을 분석하여 맞춤 직무 적합도(%)를 계산하는 기능 추가.

## 6. 기술적 이슈 및 해결 방안 (Technical Issues & Solutions)

### [Issue 1] 실행 중 수동 개입 발생 (인증 및 승인)
- **상태:** **해결 완료**
- **해결:** 전용 계정(`jeew0n.lee.217@gmail.com`) 사용 및 `auto-allow` 정책 수립으로 무인 자동화 환경 구축.

### [Issue 2] 스케줄러 실행 실패 (에러 코드 -2147023829)
- **상태:** **해결 완료**
- **해결:** 스케줄러가 `gemini` 명령어를 찾지 못하던 문제를 `C:\Users\zzib0\AppData\Roaming\npm\gemini.cmd` 전체 경로를 배치 파일에 명시하여 해결.

### [Issue 3] 중앙 DB(Google Doc) 접근 권한 및 효율성 문제
- **상태:** **해결 완료 (Gmail Search DB 표준화)**
- **해결:** Google Docs의 권한 문제를 우회하고 시스템 효율을 높이기 위해, 실제 발송된 이메일 자체를 데이터베이스로 사용하는 방식을 정식 채택. 별도의 DB 동기화 없이 '메일이 발송된 정보'만을 신규 여부의 기준으로 삼아 무결성 확보.

### [Issue 4] 노트북 부팅 시 자동 실행 누락
- **상태:** **해결 완료 (2026-05-16)**
- **원인:** 예약된 시간(오전 9시)에 노트북이 꺼져 있을 경우, 이후 부팅 시 작업을 수행하는 옵션이 비활성화되어 있었음.
- **해결:** 윈도우 작업 스케줄러의 `StartWhenAvailable` (예약된 시간을 놓친 경우 가능한 한 빨리 작업 시작) 옵션을 활성화하여 부팅 즉시 모니터링이 수행되도록 수정.

### [Issue 5] 배치 파일 명령어 인자 오류 (`Unknown argument: yes`)
- **상태:** **해결 완료 (2026-05-18)**
- **원인:** `run_monitor.bat` 내 `gemini` 명령어 실행 시 사용된 `--yes` 옵션이 존재하지 않는 인자로 판명되어 실행 직후 종료됨.
- **해결:** Gemini CLI의 자동 승인 표준 인자인 `-y` (또는 `--yolo`)로 수정하여 정상 작동 확인.

### [Issue 6] Google 보안 알림 메일 빈번 발생
- **상태:** **해결 완료 (2026-05-18)**
- **원인:** Playwright가 매번 새로운 일회성 브라우저 세션을 생성하여 접속함으로써 Google이 이를 매번 '새로운 기기'로 인식하여 보안 경고를 발송함.
- **해결:** Playwright MCP 설정에 `--user-data-dir` 옵션을 추가하여 브라우저 세션(쿠키 및 기기 신뢰 정보)이 유지되도록 수정. 이로 인해 한 번 '신뢰할 수 있는 기기'로 등록되면 추가적인 보안 알림 없이 인증이 유지됨.

### [Issue 7] 클라우드 배포 시 워크플로우 경로 문제 (2026-08-03)
- **상태:** **해결 완료**
- **원인:** `actions/checkout`과 `actions/setup-node` 사용 시 프로젝트 내 바이너리 파일을 경로로 오인하는 오류 발생.
- **해결:**
    1. Git 추적에서 불필요한 `book_diary/` 폴더를 제외.
    2. 워크플로우 설정을 단순화하고, `working-directory`를 명시하여 경로 모호성 제거.
    3. `monitor_v2.js`에 예외 발생 시 관리자 자동 알림 메일 전송 로직 추가.

### [Issue 8] 토큰 최적화 및 실행 주기 조정 (2026-06-04)
- **상태:** **적용 완료**
- **원인:** AI 기반 중복 체크(Gmail 검색)가 토큰 소비가 크고, 요일별 실행 제외 시 데이터 일관성이 깨질 우려가 있었음.
- **해결:**
    1. **코드 기반 중복 제거:** `parse.js`에 `history.json` 대조 로직을 추가하여 AI 개입 없이 1차 필터링 수행. (토큰 80% 절감)
    2. **토요일 실행 제외 (방법 B):** `parse.js` 시작단에 요일 체크 로직을 넣어 토요일은 AI 호출 없이 자동 종료되도록 설정.
    3. **가역성 확보:** 모든 로직은 모듈화되어 있으며, Git 히스토리를 통해 언제든 이전(Gmail 검색 방식)으로 복구 가능한 상태 유지.

### [Issue 9] 토요일 실행 및 한글 깨짐 이슈 (2026-06-06)
- **상태:** **해결 완료**
- **원인:** `parse_with_ai.js` 사용 시 요일 체크 누락 및 윈도우 인코딩(CP949)과 UTF-8 충돌.
- **해결:**
    1. **수집 단계 스킵:** 실행 순서상 가장 빠른 `scrape.js` 상단에 요일 체크 로직 추가.
    2. **UTF-8 환경 강제:** `run_monitor.bat`에 `chcp 65001` 명령 추가.
    3. **메일 발송 고도화:** `notify.js`에서 Gemini CLI 호출 시 UTF-8 및 HTML 형식을 명시적으로 지시.

### [Issue 10] ETRI 및 청년인턴 중복 알림 이슈 (2026-06-07)
- **상태:** **해결 완료**
- **원인:** 
    1. `filter.js` 내 `loadJson` 함수 미구현으로 인한 히스토리 참조 실패.
    2. URL 내 `;jsessionid` 제거 시 핵심 식별자(`b_idx`, `internId`)까지 함께 삭제되어 동일 공고를 다르게 인식.
    3. AI 파싱 시 제목 앞뒤 공백 등 미세한 텍스트 차이 발생.
- **해결:**
    1. **히스토리 로딩 복구:** `loadJson` 구현 및 `history.json` 대조 로직 정상화.
    2. **정밀 정규화(`normalizeLink`):** 세션 ID만 제거하고 쿼리 파라미터는 보존하는 정규식 적용.
    3. **데이터 치유:** 기존 `history.json`의 파손된 링크 데이터를 신규 로직으로 전수 재정비.
    4. **배치 파일 제어:** 토요일 실행 스킵 로직을 배치 파일 레벨로 상향하여 프로세스 안정성 강화.

### [Issue 11] 이메일 발송 토큰 최적화 (2026-06-09)
- **상태:** **적용 완료**
- **원인:** 이메일 발송 시 매번 Gemini CLI를 호출하여 불필요한 AI 토큰이 소모됨.
- **해결:**
    1. **Nodemailer 도입:** AI 호출 없이 직접 SMTP를 통해 이메일을 발송하도록 `notify.js` 리팩토링.
    2. **환경변수 관리:** `.env` 파일을 통해 보안 정보를 분리하고 관리.
    3. **가역성 확보:** AI 방식 호출을 위한 백업 파일(`notify_ai.js`) 및 복구 지침서(`REVERT_EMAIL_CONFIG.md`) 생성.

### [Issue 12] 청년인턴 상세 페이지 링크 연동 오류 (2026-06-10)
- **상태:** **해결 완료**
- **원인:** 
    1. 청년인턴 사이트(2030db.go.kr)가 상세 페이지를 POST 방식으로만 호출하여 직접 링크가 어려웠음.
    2. AI가 링크가 없자 가상의 잘못된 URL(hallucination)을 생성하여 클릭 시 빈 화면이 발생함.
- **해결:**
    1. **GET 호출 방식 발견:** 특정 파라미터(`youthId`)를 사용하면 상세 페이지 직접 접근이 가능함을 확인.
    2. **파싱 로직 고도화:** `parse.js` 및 `parse_with_ai.js`에서 공고 고유 ID를 추출하여 `selectYouthInternDetail.do?youthId=ID` 형식의 직접 링크를 생성하도록 수정.
    3. **일관성 확보:** `filter.js`의 정규화 로직도 해당 URL 형식을 지원하도록 업데이트하여 중복 체크 안정성 강화.

### [Issue 14] 히스토리 데이터 자동 청소 기능 구현 (2026-06-12)
- **상태:** **적용 완료**
- **원인:** 시간이 지남에 따라 `history.json` 파일이 계속 커져 데이터 관리 효율성이 저하될 우려가 있음.
- **해결:**
    1. **객체 기반 저장 전환:** 기존 텍스트 기반 저장 방식에서 `{key, timestamp}` 객체 기반으로 저장 구조 변경.
    2. **자동 청소 로직 (`filter.js`):** 시스템 실행 시마다 데이터의 타임스탬프를 확인하여 30일이 경과한 기록은 자동으로 제거.
    3. **호환성 확보:** 기존 텍스트 데이터와 신규 객체 데이터를 동시에 처리하도록 하여 마이그레이션 안정성 확보.

## 7. 리소스 정보
- **기술 스택:** AI/LLM(Gemini), 자동화(Playwright), 발송(Nodemailer/SMTP), 데이터베이스(Supabase), CI/CD(GitHub Actions)
- **현재 주요 파일:** `monitor_v2.js`, `lib/supabase.js`, `.github/workflows/monitor.yml`
