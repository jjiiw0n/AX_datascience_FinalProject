# 지능형 웹 모니터링 자동화 프로젝트

## 1. 프로젝트 개요
지정한 웹사이트를 주기적으로 모니터링하여 새로운 게시물을 감지하고, 이를 지정된 전용 계정으로 기록 및 알림을 보내는 자동화 시스템 구축.

## 2. 시스템 아키텍처
- **탐색 (Playwright MCP):** 보안 및 봇 방지 정책을 우회하여 대상 사이트의 최신 게시물 데이터를 수집.
- **데이터베이스 (Gmail Search DB):** Google Docs의 권한 및 세션 유지 문제를 근본적으로 해결하기 위해, 실제 발송된 메일 이력(`"[통합 알림]"`)을 전체 메일함에서 검색하여 중복 수집을 방지하는 **'Event-Log 기반 DB'** 전략 채택. 제목, 링크, 등록일 3종 교차 검증을 통해 정확도 향상.
- **알림 (Gmail):** 신규 게시물 발생 시 상세 내역을, 없을 시에는 시스템 정상 작동 확인(Heartbeat) 메일을 전송.
- **전략 (Gemini Skills):** 복잡한 수집 로직을 `integrated-web-monitor` 폴더 내에 사이트별(`sites/`) 및 인프라별(`infra/`) 모듈로 분리하여 관리.
- **버전 관리 (Git):** Feature Branch 전략을 사용하여 기능별 독립적인 개발 및 검증 수행.

## 3. 구축 단계
1. **인증 및 보안 설정:** 전용 모니터링 계정 분리 및 OAuth 2.0 자동화 환경 구축.
2. **수집 로직 최적화:** Playwright를 이용한 사이트별 맞춤 크롤링 엔진 구축.
3. **모듈형 Architecture 설계:** 사이트별(`etri`, `kt`, `btp-talent`, `youth-intern`) 로직 분리.
4. **알림 시스템 고도화:** 모든 사이트 결과를 하나로 모으는 **'통합 리포트'** 및 상세 중복 필터링 구현.
5. **스케줄러 안정화:** 미실행 방지를 위한 부팅 즉시 실행(Catch-up) 옵션 적용.

## 4. 현재 진행 상태 및 확인 완료 사항
- **진행 상태:** 
  - [x] 통합 웹 모니터링 시스템(`integrated-web-monitor`) 복구 및 Gmail 검색 기반 로직 표준화 완료.
  - [x] 전용 모니터링 계정(`jeew0n.lee.217@gmail.com`) 발신/수신 일원화 및 인증 완료.
- [x] **에이전트 기반 자율 주행 도입:** `web-monitor-agent`를 생성하여 메인 세션과 독립된 실행 환경 구축 완료.
  - [x] **배치 파일 고도화:** `run_monitor.bat`이 전용 에이전트를 직접 호출하도록 수정하여 무인 자동화 안정성 강화.
  - [x] **계정 권한 및 범위 제한:** `jeew0n.lee.217@gmail.com` 전용 계정 및 특정 디렉토리로 작업 범위 한정(보안 강화).
  - [x] **알림 주기 및 안정성 최적화:** 매일 오전 11:00 실행 및 **노트북 부팅 시 즉시 실행 옵션(StartWhenAvailable) 적용 완료.**
  - [x] **배치 파일 명령어 오류 수정 (2026-05-18):** 잘못된 인자(`--yes`)를 정식 인자(`-y`)로 수정하여 스케줄러 실행 안정성 확보.

## 5. 향후 로드맵 (Future Roadmap)
- **[Step 1] 모니터링 대상 지속 확장:** 관심 기업 및 공공기관 공고 추가 등록.
- **[Step 2] 구글 인증 세션 안정화:** 백그라운드 무인 실행 시 구글 OAuth 세션 유지 여부 지속 모니터링.
- **[Step 3] AI 직무 적합성 분석:** Agent가 공고 내용을 분석하여 맞춤 직무 적합도(%)를 계산하는 기능 추가.

- **리소스 정보:**
  - **대상 사이트:** ETRI, KT Careers, 부산테크노파크, 2030 청년인턴
  - **발신/수신 계정:** jeew0n.lee.217@gmail.com
  - **정기 실행 시각:** 매일 오전 11:00 (노트북을 늦게 켤 경우 부팅 즉시 실행)
  - **실행 명령어:** `run_monitor.bat` (스케줄러 등록 명칭: `GeminiWebMonitor`)

## 6. 기술적 이슈 및 해결 방안 (Technical Issues & Solutions)

### [Issue 1] 실행 중 수동 개입 발생 (인증 및 승인)
- **상태:** **해결 완료**
- **해결:** 전용 계정(`jeew0n.lee.217@gmail.com`) 사용 및 `auto-allow` 정책 수립으로 무인 자동화 환경 구축.

### [Issue 2] 스케줄러 실행 실패 (에러 코드 -2147023829)
- **상태:** **해결 완료**
- **해결:** 스케줄러가 `gemini` 명령어를 찾지 못하던 문제를 `C:\Users\zzib0\AppData\Roaming\npm\gemini.cmd` 전체 경로를 배치 파일에 명시하여 해결.

### [Issue 3] 중앙 DB(Google Doc) 접근 권한 및 효율성 문제
- **상태:** **해결 완료 (Gmail Search DB 표준화)**
- **해결:** Google Docs의 권한 문제를 우회하고 시스템 효율을 높이기 위해, 실제 발송된 이메일 자체를 데이터베이스로 사용하는 방식을 정식 채택. 별도의 DB 동기화 없이 '메일이 발송된 정보'만을 신규 여부의 기준으로 삼아 무결성 확보.

### [Issue 4] 노트북 부팅 시 자동 실행 누락
- **상태:** **해결 완료 (2026-05-16)**
- **원인:** 예약된 시간(오전 9시)에 노트북이 꺼져 있을 경우, 이후 부팅 시 작업을 수행하는 옵션이 비활성화되어 있었음.
- **해결:** 윈도우 작업 스케줄러의 `StartWhenAvailable` (예약된 시간을 놓친 경우 가능한 한 빨리 작업 시작) 옵션을 활성화하여 부팅 즉시 모니터링이 수행되도록 수정.

### [Issue 5] 배치 파일 명령어 인자 오류 (`Unknown argument: yes`)
- **상태:** **해결 완료 (2026-05-18)**
- **원인:** `run_monitor.bat` 내 `gemini` 명령어 실행 시 사용된 `--yes` 옵션이 존재하지 않는 인자로 판명되어 실행 직후 종료됨.
- **해결:** Gemini CLI의 자동 승인 표준 인자인 `-y` (또는 `--yolo`)로 수정하여 정상 작동 확인.

### [Issue 6] Google 보안 알림 메일 빈번 발생
- **상태:** **해결 완료 (2026-05-18)**
- **원인:** Playwright가 매번 새로운 일회성 브라우저 세션을 생성하여 접속함으로써 Google이 이를 매번 '새로운 기기'로 인식하여 보안 경고를 발송함.
- **해결:** Playwright MCP 설정에 `--user-data-dir` 옵션을 추가하여 브라우저 세션(쿠키 및 기기 신뢰 정보)이 유지되도록 수정. 이로 인해 한 번 '신뢰할 수 있는 기기'로 등록되면 추가적인 보안 알림 없이 인증이 유지됨.

### [Issue 7] 중복 발송 및 메일 개별 발송 문제
- **상태:** **해결 완료 (2026-05-20)**
- **원인:** 사이트별 개별 메일 발송으로 인한 피로도 증가 및 제목 위주의 단순 중복 검사로 인한 오탐 발생.
- **해결:** 모든 사이트 결과를 하나의 **'[통합 알림] 웹 모니터링 결과 보고'** 메일로 통합 발송하도록 로직 변경. 중복 검사 시 '제목+링크+등록일' 3종 교차 검증을 도입하고, Gmail 전체 메일함 검색을 통해 과거 이력을 보다 정밀하게 대조함. ETRI의 특정 과거 공고(4/28, 5/8, 5/12)에 대한 강제 예외 처리 규칙 추가.
